# webchat
Simple webchat based on websockets. Java 8 + Jetty embed.


Build:
```
$ maven compile
$ maven assembly:single
```

Run:
```
$ java -jar chat.jar
```

Watch (in browser):

```
localhost:8080
```
